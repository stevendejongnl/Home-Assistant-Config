# =============================================================================
# Notification Groups
# Named notification targets that bundle one or more mobile-app services.
# =============================================================================

- platform: group
  name: steven
  services:
    - service: mobile_app_steven

- platform: group
  name: anja
  services:
    - service: mobile_app_iphone_van_anja

- platform: group
  name: family
  services:
    - service: steven
    - service: anja

- platform: group
  name: security
  services:
    - service: steven
